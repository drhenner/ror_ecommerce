require 'spec_helper'

describe Payment, ".mark_invoice_complete" do
  pending "test for mark_invoice_complete"
end

describe Payment, "#store( credit_card, options = {})" do
  pending "test for store( credit_card, options = {})"
end

describe Payment, "#unstore( profile_key, options = {})" do
  pending "test for unstore( profile_key, options = {})"
end

describe Payment, "#authorize(amount, credit_card, options = {})" do
  pending "test for authorize(amount, credit_card, options = {})"
end

describe Payment, "#capture(amount, authorization, options = {})" do
  pending "test for capture(amount, authorization, options = {})"
end

describe Payment, "#charge( amount, profile_key, options ={})" do
  pending "test for charge( amount, profile_key, options ={})"
end

describe Payment, "#unique_order_number" do
  pending "test for unique_order_number"
end

describe Payment, "#validate_card( credit_card, options ={})" do
  pending "test for validate_card( credit_card, options ={})"
end

describe Payment, "#process(action, amount = nil)" do
  pending "test for process(action, amount = nil)"
end
