require 'spec_helper'

describe Product, ".featured_image" do
  pending "test for featured_image"
end

describe Product, ".price" do
  pending "test for price"
end

describe Product, ".set_keywords=(value)" do
  pending "test for set_keywords=(value)"
end

describe Product, ".set_keywords" do
  pending "test for set_keywords"
end

describe Product, ".primary_image_url(image_size = :small)" do
  pending "test for primary_image_url(image_size = :small)"
end

describe Product, ".display_price_range(j = ' to ')" do
  pending "test for display_price_range(j = ' to ')"
end

describe Product, ".price_range" do
  pending "test for price_range"
end

describe Product, ".last_master_variant" do
  pending "test for last_master_variant"
end

describe Product, "#standard_search(args, params)" do
  pending "test for standard_search(args, params)"
end

describe Product, "#featured" do
  pending "test for featured"
end

describe Product, "#admin_grid(params = {}, active_state = nil)" do
  pending "test for "
end
