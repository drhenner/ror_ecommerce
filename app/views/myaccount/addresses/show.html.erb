<h1> Address </h1>

<p>
  <strong>Name:</strong>
  <%= @address.name %>
</p>

<p>
  <%= link_to "Edit", edit_myaccount_address_path(@address), :class => 'button' %> |
  <%= link_to "Destroy", myaccount_address_path(@address), :confirm => 'Are you sure?', :method => :delete, :class => 'button' %> |
  <%= link_to "View All", myaccount_addresses_path, :class => 'button' %>
</p>
