<h1> New Address </h1>

<%= form_for(@address, :url => myaccount_addresses_path(@address)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

  <%= link_to 'Back to List', myaccount_addresses_path, :class => 'button ar-small-black-button' %>
