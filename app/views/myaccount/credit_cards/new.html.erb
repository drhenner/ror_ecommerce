<h3 class="text-xl font-medium mb-6">New Credit Card</h3>

<%= form_for(@credit_card, as: :credit_card, url: myaccount_credit_cards_path) do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
<% end %>

<div class="mt-4">
  <%= link_to 'Back to List', myaccount_credit_cards_path, class: "text-sm text-[var(--color-accent)] hover:underline" %>
</div>
