<%= render :partial => '/admin/merchandise/wizards/sub_header'%>
<% if  @product_types.empty? %>
  <h2>New Product Type</h2>
<% else %>
  <h2>Pick a Product Type</h2>
<% end %>

<div id='shopping_addresses_selections' class='span-18 last'>
  <% @product_types.each do |product_type| %>
    <div class='span-8 box <%= cycle( '', 'last') %> <%= 'selected' if selected?(product_type.id) %> '>
      <label><%= product_type.name %></label><br/>
      <%= button_to 'Use', admin_merchandise_wizards_product_type_path(product_type),
                    :class => "button spade inline-block  ",
                    :method => :put %>
    </div>
  <% end %>

</div>
<hr style='color:#aac'/>
<br />

<%= simple_form_for @product_type, :url => admin_merchandise_wizards_product_types_path do |product_type_form| %>
  <%= render :partial =>  'form', :locals => {:form => product_type_form}  %>
<% end %>