<% if @shipping_category.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@shipping_category.errors.count, "error") %> prohibited this admin_config_shipping_category from being saved:</h2>

    <ul>
    <% @shipping_category.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<label>Name</label>
<%= f.text_field :name %>

<%= f.submit "Create Shipping Category", class: "button" %>
