<%= render :partial => '/admin/merchandise/sub_header'%>

<h1> Brands </h1>
<div class=' pretty_table'>
<table>
  <thead>
  <tr>
    <th>Name</th><th></th>
  </tr>
  </thead>
  <% for brand in @brands %>
    <tr>
      <td><%= link_to brand.name, admin_merchandise_brand_path(brand) %></td>
      <td><%= link_to "Edit", edit_admin_merchandise_brand_path(brand) %>
      <%= link_to "Delete", admin_merchandise_brand_path(brand), confirm: 'Are you sure?', method: :delete %></td>
    </tr>
  <% end %>
</table>
</div>

<p><%= link_to "New Brand", new_admin_merchandise_brand_path, :class => 'button' %></p>
