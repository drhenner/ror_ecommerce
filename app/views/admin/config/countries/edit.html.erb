<h3>Edit <%= @country.name %> </h3>

<%= form_for(@country, url: admin_config_country_path(@country)) do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
<% end %>

<%= link_to 'Back', admin_config_countries_path, class: 'button' %>
