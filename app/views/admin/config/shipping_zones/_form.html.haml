- if @shipping_zone.errors.any?
  #error_explanation
    %h2
      = pluralize(@shipping_zone.errors.count, "error")
      prohibited this Shipping Zone from being saved:
    %ul
      - @shipping_zone.errors.full_messages.each do |msg|
        %li= msg
%fieldset
  %label Name
  = f.text_field :name
.actions
  = f.submit "Create", :class => "button"
