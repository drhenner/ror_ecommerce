<%= render :partial => '/admin/config/sub_header'%>

<h1> Shipping Zones</h1>

<table class='span-12'>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
  </tr>

<% @shipping_zones.each do |shipping_zone| %>
  <tr>
    <td><%= shipping_zone.name %> </td>
    <td><%= link_to 'Show', admin_config_shipping_zone_path(shipping_zone) %></td>
    <td><%= link_to 'Edit', edit_admin_config_shipping_zone_path(shipping_zone) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shipping zone', new_admin_config_shipping_zone_path %>
