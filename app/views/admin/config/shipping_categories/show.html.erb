<div class="page-header">
  <h1 class="page-title"><%= @shipping_category.name %></h1>
  <div class="page-actions">
    <%= link_to 'Edit', edit_admin_config_shipping_category_path(@shipping_category), class: 'btn btn-primary' %>
    <%= link_to 'Back', admin_config_shipping_categories_path, class: 'btn btn-outline' %>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <p id="notice"><%= notice %></p>
  </div>
</div>
