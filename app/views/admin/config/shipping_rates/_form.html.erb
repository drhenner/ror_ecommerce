<% if @shipping_rate.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@shipping_rate.errors.count, "error") %> prohibited this Shipping Rate from being saved:</h2>
    <ul>
      <% @shipping_rate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= f.association :shipping_method, :collection => @shipping_methods %>
<%= f.input :rate %>
<%= f.input :minimum_charge %>
<%= f.association :shipping_rate_type, :collection => @shipping_rate_types %>
<%= f.association :shipping_category, :collection => @shipping_categories %>
<div class="actions">
  <%= f.submit %>
</div>
