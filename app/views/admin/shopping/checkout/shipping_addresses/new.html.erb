<div class="page-header">
  <div>
    <h1 class="page-title">New Shipping Address</h1>
  </div>
  <div class="page-actions">
    <%= link_to "Back to #{truncate(session_admin_cart.customer&.name || 'Customer', length: 35)}'s Cart", admin_shopping_products_path, class: 'btn btn-outline btn-sm' %>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <%= render partial: 'form' %>
    <%= link_to 'Back', admin_shopping_checkout_shipping_addresses_path, class: 'btn btn-outline btn-sm mt-3' %>
  </div>
</div>
