%h1 New Referral
= form_for @referral, :url => admin_user_datas_referrals_path(@referral) do |f|
  = render :partial => 'form', :locals => {:f => f}
  .actions= f.submit 'Create' , :class => 'button'
%p= link_to "Back to List", admin_user_datas_referrals_path, :class => 'button'
