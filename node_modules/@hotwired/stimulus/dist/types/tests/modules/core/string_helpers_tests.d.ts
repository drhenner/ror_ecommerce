import { TestCase } from "../../cases/test_case";
export default class StringHelpersTests extends TestCase {
    "test should camelize strings"(): void;
    "test should namespace camelize strings"(): void;
    "test should dasherize strings"(): void;
    "test should capitalize strings"(): void;
    "test should tokenize strings"(): void;
}
